# Persistent Delta 当前实现

## 当前状态

- 基线：`d609843c4caf24fe00845423240cb2cb7066a006`（本次 fetch 后的 `origin/main`）
- 分支：`persistent_delta`
- 提交约定：相对主线只保留一个功能提交，后续提交修改使用 amend。
- 已实现范围：顶层 `StandardColumnData` 的 `INTEGER`/`BIGINT`，同时持久化 value 与 validity 的累计 delta。
- 存储格式：新增 storage version `v2.1.0`（70），delta 描述放在标准列 descriptor 的 field 104；旧版本只走原有完整物化路径。

## 代码形状

- `UpdateSegment` 在 segment lock 内维护 dirty 标记，导出/恢复累计 root，不把持久 root 当作事务 undo 或 WAL 节点。
- `HasUncheckpointedChanges()` 只参与 metadata reuse；原 `HasChanges()` 保留给完整 checkpoint/checkpointer，避免 clean delta 在禁止保留时被跳过物化。
- value、validity 的位置和数据流分别使用现有列压缩设施。生成 delta 只接受正式 FULL checkpoint、FULL partial-block manager、已确定的可见性边界和支持 `v2.1.0` 的顶层数值列；base 存在 transient segment、阈值不满足或 `byte_size` 不可靠时立即回退完整物化。
- clean snapshot 在格式和位置条件允许时复用完整旧列对象。生成新结果列时 value/validity 两侧独立构造，辅助 checkpoint state 在首次登记 partial block 前注册 owner，并立即保存 immutable descriptor，避免最终 flush 前访问 transient segment。
- descriptor-only 反序列化用于 metadata block 计算和完整 block 校验；`WriteHeader()` 后的 `VerifyBlocks()` 额外遍历 value/validity delta descriptor 的 block 引用，不做全局去重。
- 普通 UPDATE、WAL replay、ROLLBACK 只使运行期 root 标记 dirty，旧 snapshot descriptor 保留到替换或完整物化结束。

## 当前验证

本次 rebase 无冲突，合并两次提交后与原补丁的 `patch-id` 一致，`git diff --check` 通过。新基线尚未重新构建或运行测试；rebase 前的构建和测试证据见 [实现历史.md](实现历史.md)。

既有构建配置：

```text
cmake -S . -B build/reldebug-abi -G Ninja \
  -DCMAKE_C_COMPILER=/opt/rh/devtoolset-11/root/usr/bin/gcc \
  -DCMAKE_CXX_COMPILER=/opt/rh/devtoolset-11/root/usr/bin/g++ \
  -DCMAKE_C_COMPILER_LAUNCHER= -DCMAKE_CXX_COMPILER_LAUNCHER= \
  -DDUCKDB_EXPLICIT_PLATFORM=linux_amd64_gcc11 \
  -DCMAKE_CXX_FLAGS=-D_GLIBCXX_USE_CXX11_ABI=1
cmake --build build/reldebug-abi --config RelWithDebInfo -j 2
```

## 尚未执行

未运行完整 `make allunit`、sanitizer 矩阵、故障注入全套和性能 benchmark。性能脚本/实验仍属于单独授权步骤；当前实现没有把 focused 结果写成全量验收结论。旧 reader 拒绝新版本、共享 block 的独立确定性 fixture、`byte_size` 缺失 fixture 和并发 checkpoint 压测仍需补充。

## 文档关系

- [plan.md](plan.md)：实现契约、范围、正确性不变量和验收边界。
- [实现文档.md](实现文档.md)：只保留当前代码、当前验证和当前限制。
- [实现历史.md](实现历史.md)：追加有意义的设计调整、失败尝试和验证经验，不替代当前状态。
