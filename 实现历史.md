# Persistent Delta 实现历史

本文件只追加有意义的实现尝试、设计调整、失败原因和验证证据，不作为当前状态文档。当前状态以 [实现文档.md](实现文档.md) 为准。

## 2026-09-18：实现启动前的基线核对

- 基线为 `390ba4114e27564cd869cc812a14f47e1537d711`，仓库尚无 persistent-delta 代码。
- 当前 `StandardColumnData::Checkpoint()` 同时驱动 value/validity checkpoint state；`ColumnDataCheckpointer` 以 `HasChanges()` 决定是否真正物化，因此 clean persisted delta 的 metadata reuse 判断不能替代原有物化语义。
- `UpdateSegment` 的 dirty 标记必须由 segment lock 保护，避免 rollback 路径反向获取 `ColumnData::update_lock`。
- 默认 `CheckpointOptions` 的 FULL 类型不能单独证明正式磁盘 checkpoint；optimistic writer 使用 `APPEND_TO_TABLE` partial-block manager，资格判断需要同时检查来源和 `PartialBlockType`。
- `PersistentColumnData` 的 descriptor 由 `InitializeColumn()` 消费并移动字段，长期 snapshot descriptor 与可消耗加载描述必须分离。
- metadata-only 校验路径不能恢复运行期 delta root；最终 `VerifyBlocks()` 还需要从持久 delta descriptor 计入辅助 block 引用。

暂无代码尝试、构建结果或测试结果。

## 2026-09-19：首轮实现与验证经验

- 首次增量构建发现头文件已改为 `optional<PersistentColumnData>`，实现仍引用未定义的 `PersistentColumnDataPointer`。统一为可移动 optional descriptor，并保留每个辅助流的独立逻辑类型上下文。
- 首次多 row group smoke 在第三次 checkpoint 触发 `Attempting to call ColumnSegment::GetDataPointer on a transient segment`。原因是辅助列的 partial block 尚未经过 collection-level flush，`ToPersistentData()` 却从 transient `ColumnData` 重新取 descriptor。修复为辅助 checkpoint state 在返回前保存独立 descriptor，结果列继续由 owner bundle 保活到最终 flush。
- clean delta 的复用仍走完整旧列对象；需要生成新结果时 value/validity 两侧都创建独立对象，避免 `SetValidityData()` 改写旧对象图。
- 在未限制父列的尝试中，整数列的 delta 逻辑可能进入嵌套 STRUCT 路径，重启后懒加载出现统计初始化错误。新增顶层列 gate（`HasParent()` 回退原路径）后，新增 STRUCT clean-delta 回归通过。
- `DebugVerifyBlocksSetting` 的 delta 引用统计必须放在每张表的 segment 枚举之外；否则会按每个 base segment 重复累加 delta。修正后带 value/validity 辅助流的最终 `VerifyBlocks()` smoke 通过。
- 最终 block 校验改为从列 metadata 的 field 104 直接收集辅助流 block，未加载列不恢复 delta root；没有持久列起点的临时 row group 才回退运行期遍历。
- 新增 storage version 后补跑 `generate_enum_util.py`，使 `V2_1_0` 在 `EnumUtil` 的字符串转换和诊断路径中可见。
- 通过 GCC 11、C++17、`_GLIBCXX_USE_CXX11_ABI=1` 的 `build/reldebug-abi` 构建；focused storage/struct/persistent-delta tests 通过。完整单元、sanitizer、性能实验尚未执行。

## 2026-09-19：review 修复

- delta eligibility 现在先由 `TryPreparePersistentDelta()` 完成完整准入：value/validity 两侧必须全是 persistent，`byte_size` 必须可靠，导出和阈值检查成功后才进入辅助流压缩；失败统一回到原完整 checkpoint，避免 transient base 触发 `GetDataPointer()` 异常或读取未初始化 size。
- `PersistentUpdateData::Validate` 改名为 `ValidateDescriptorTypes`，只承担 descriptor 配对和逻辑类型检查；位置、长度、NULL 和边界仍由 `CheckpointUpdateData::Validate` 及恢复读取阶段负责。
- `PartialBlockManager::Merge()` 同步转移 checkpoint owner，保证 optimistic writer 合并 partial blocks 时 owner 与 block 引用保持同一生命周期。
- persistent delta SQL 回归从 82 增至 90 assertions，新增追加 transient base 后 UPDATE、CHECKPOINT、restart 验证。

## 2026-09-19：更新主线并保持单提交

- 将基于 `390ba4114e` 的两个功能提交无冲突 rebase 到 `origin/main` 的 `d609843c4caf24fe00845423240cb2cb7066a006`，合并为一个功能提交；后续提交修改使用 amend。
- 更新文档前，rebase 前后累计补丁的 stable patch-id 均为 `2a642eb8f6754517df8e32217e25a4a8a1860279`，`git diff --check` 通过。补丁一致性检查不代替在新基线重新构建或运行测试。
- 以下结果来自 rebase 前的验证记录：GCC 11 ABI 构建通过；`test/sql/storage/persistent_delta.test` 90 assertions；`partial_blocks/many_columns_validity.test` 1206 assertions；`metadata/partial_column_metadata_reuse_small_block.test` 11 assertions；`metadata/partial_column_metadata_reuse_drop_column.test` 15 assertions；`types/struct/struct_storage.test` 25 assertions。
- rebase 前手工检查已覆盖多 row group 非 constant delta、过滤统计、WAL replay 后再次 checkpoint、`STORAGE_VERSION 'v2.0.0'` 完整物化回退。本轮没有重新构建或运行测试，当前验证状态在实现文档中单独标明。
